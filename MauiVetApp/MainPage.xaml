<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MauiVetApp.Models"

             x:Class="MauiVetApp.MainPage">
    <ContentPage.BindingContext>
        <local:MainPageModel />
    </ContentPage.BindingContext>
    <ScrollView>
        <VerticalStackLayout
            Spacing="25"
            Padding="30,0"
            VerticalOptions="Center">
            <Button Text="Vis dyr" Clicked="ShowAnimals_Btn">
            </Button>
            <Button Text="Statistik" Clicked="ShowStatistic_btn_Clicked">
            </Button>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
